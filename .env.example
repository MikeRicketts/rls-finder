# Template for RLScout's .env (copy to .env and fill in). The real .env is
# gitignored so your keys never get committed.

# Anthropic API key for the optional --ai layer (triage + recon fallback).
ANTHROPIC_API_KEY=
